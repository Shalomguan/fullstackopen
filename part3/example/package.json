{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "author": "snake",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd /home/ceiling/fullstackopen/part2/example && npm run build && cp -r dist /home/ceiling/fullstackopen/part3/example",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs",
    "test": "echo \\\"Error: no test specified\\\" && exit 1"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10"
  }
}
